services:
  api:
    build: .
    ports:
      - "3001:3000"
    environment:
      DB_HOST: db
      DB_USER: postgres
      DB_PASS: MyS3cur3P@ss
      DB_NAME: recipes_db
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: MyS3cur3P@ss
      POSTGRES_DB: recipes_db
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: